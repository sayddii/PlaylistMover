# Used Libraries 📚

This project was built using the following Python libraries:

*   **CustomTkinter:** For the modern, dark-themed graphical user interface (GUI).
*   **Spotipy:** A lightweight Python library for the Spotify Web API.
*   **Google Client Library:** Official library for accessing YouTube Data API v3.
*   **Pillow (PIL):** For handling images and icons.
*   **PyInstaller:** Used to compile the Python script into a standalone `.exe` file.